# @package _global_
defaults:
  - override /model: "rf"
  - override /loss: "portfolio"
  - override /training: "sklearn"

experiment_name: "rf"

# Cross-sectional models use no input window; purge only the 2-day forward-return horizon.
split:
  purge_size: 2
  lookback: 0
